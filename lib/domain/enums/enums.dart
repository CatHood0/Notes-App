enum TypeUser {
  admin,
  guess,
  user,
  unknown,
}

enum TypeBan{
  perRobTemplates,
  perPublicSensitiveContent,
  nothing,
}

enum TypeSort {
  title,
  creation,
  modification,
  recent,
  suggested,
  anything,
}

enum AppTheme{
  darkTheme,
  lightTheme,
}